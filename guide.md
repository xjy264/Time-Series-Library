# AEMO VIC1 净负荷研究指南

## 1. 文档用途

本文件只负责“研究做什么、为什么这么做、结果说明什么”。

- `guide.md` 负责研究方向、问题定义、周期性结论、实验优先级和后续判断标准。
- `AGENTS.md` 负责实现层面的执行说明，例如代码怎么写、脚本怎么跑、数据放哪里、产物怎么保存。
- 如果两份文档出现冲突，以职责更具体的文档为准。
  - 研究假设、特征边界、实验路线、结论解释，以 `guide.md` 为准。
  - 代码路径、脚本参数、处理流程、落盘位置，以 `AGENTS.md` 为准。

## 2. 当前研究主线

当前主线不是“单纯堆模型”，而是先证明净负荷里存在强周期结构，再围绕周期结构做预测。

核心判断是：

- 净负荷不是随机序列。
- 日周期、周周期、月周期都很强。
- 很多模型的提升空间，首先来自“把周期建模对”，其次才是更复杂的注意力或更大的外生变量集。

因此，当前研究顺序应当是：

1. 先证明周期性。
2. 再做周期基线。
3. 再做纯内生预测基线。
4. 再看外生变量是否真的提供增益。
5. 最后才讨论 backbone 改造。

`TimeXer` 仍然是重要候选，但不是唯一主角。`PatchTST`、`TimeMixer`、`TFT`、`TiDE`、`iTransformer` 都可以作为对照或改造灵感。

## 3. 任务与数据口径

### 3.1 预测目标

固定定义：

```text
net_load = totaldemand_mw_avg - uigf_mw_avg
```

含义：

- `totaldemand_mw_avg` 表示系统总需求。
- `uigf_mw_avg` 表示系统层面的间歇性发电预测量。
- 这个定义比直接预测总需求更贴近系统净需求压力。

### 3.2 数据来源

- AEMO VIC1 小时级系统数据：`data/aemo_vic1_hourly_2022-08-25_2025-08-24.csv`
- NOAA Melbourne Olympic Park 小时级天气数据：`data/noaa_globalhourly_melbourne_olympic_park_hourly_2022-08-25_2025-08-24.csv`

统一对齐键：

- `timestamp_local_hour`

统一粒度：

- 小时级

## 4. 已复现结果

### 4.1 周期性证据

周期分析覆盖：

- 样本数：`26301`
- 时间范围：`2022-08-25 00:00:00` 到 `2025-08-24 23:00:00`

主要结果：

| 指标 | 结果 |
| --- | ---: |
| 日内峰值小时 | 19 |
| 日内低谷小时 | 13 |
| 日内振幅 | 2432.425 MW |
| 周内峰值 | 周一 |
| 周内低谷 | 周日 |
| 周振幅 | 648.125 MW |
| 月内峰值 | 6 月 |
| 月内低谷 | 12 月 |
| 月振幅 | 1518.0 MW |
| `lag=24h` ACF | 0.6163 |
| `lag=168h` ACF | 0.4369 |

时间口径检查：

- 存在 3 个夏令时导致的 2 小时跳跃。
- 没有发现重复小时。

解释：

- 这个序列已经在模型之前表现出很强的日周期和周周期。
- 周期结构本身就能解释相当一部分误差，说明“先做周期建模”是对的。

### 4.2 周期基线

使用最后 12 个月作为留出集，只用历史周期均值，不引入复杂模型。

| Baseline | MSE | MAE |
| --- | ---: | ---: |
| `global_mean` | 2958823.0851 | 1365.6433 |
| `hour_mean` | 2293348.2876 | 1209.0672 |
| `hour_dow_mean` | 2196449.9693 | 1192.1976 |
| `hour_dow_month_mean` | 1843942.6763 | 1070.3886 |

结论：

- 只加小时、星期、月份这类周期索引，误差就已经明显下降。
- 说明净负荷的主要信号不是“复杂非线性”，而是“周期结构 + 少量残差修正”。

### 4.3 多模型基准

实验设置：

- 任务：`long_term_forecast`
- 目标：`net_load`
- 输入长度：`seq_len = 168`
- 预测长度：`pred_len ∈ {24, 48, 96}`
- 模式：`MS`

说明：

- 下面的模型结果原本是在标准化空间里计算的。
- 这里统一按训练集 `net_load` 的标准差 `1436.326 MW` 反标准化到原始 MW 尺度，方便和周期基线直接比较。

总体平均结果（MW 尺度）：

| 模型 | 平均 MSE | 平均 MAE |
| --- | ---: | ---: |
| `DLinear` | 1659503 | 1007.9 |
| `PatchTST` | 1769875 | 1033.3 |
| `TimeMixer` | 1777921 | 1032.3 |
| `TimeXer` | 1805153 | 1039.9 |
| `iTransformer` | 1911193 | 1080.7 |
| `Informer` | 2313072 | 1169.6 |
| `Autoformer` | 2332464 | 1225.0 |

分 horizon 最优模型：

| `pred_len` | 最优模型 | MSE | MAE |
| --- | --- | ---: | ---: |
| 24 | `DLinear` | 1284541 | 876.3 |
| 48 | `DLinear` | 1700128 | 1022.9 |
| 96 | `DLinear` | 1993824 | 1125.9 |

结论：

- 当前最强基线是 `DLinear`。
- `TimeXer` 没有输得很远，但也没有拿到最优。
- 复杂注意力结构并没有自动带来更好结果，说明当前瓶颈更像是“周期表达不够直接”，而不是“模型容量不够大”。

### 4.4 `TimeXer` 天气增强结果

旧版 `TimeXer`（无 NOAA）：

| `pred_len` | MSE / MAE |
| --- | --- |
| 24 | 1363252 / 891.8 |
| 48 | 1889531 / 1067.2 |
| 96 | 2162883 / 1160.6 |

新版 `TimeXer`（含 NOAA）：

| `pred_len` | MSE / MAE |
| --- | --- |
| 24 | 1420604 / 917.1 |
| 48 | 1888912 / 1071.5 |
| 96 | 2230963 / 1187.4 |

结论：

- 这版 NOAA 特征没有稳定提升 `TimeXer`。
- 说明当前天气变量更像弱修正项，不足以替代更强的辐照度、云量或太阳位置信息。

## 5. 下一步要复现的实验

### 5.1 必复现

1. 周期性分析。
2. 周期基线。
3. 纯内生模型基准。
4. `TimeXer + NOAA` 对比。

### 5.2 建议复现

1. `PatchTST`
2. `TimeMixer`
3. `iTransformer`
4. `Informer`
5. `Autoformer`

### 5.3 后续分层验证

建议把同一组实验再切成下面几个子场景：

- 工作日 vs 周末
- 夏季 vs 冬季
- 高峰 vs 低谷
- 日周期强段 vs 弱段
- 夏令时切换附近

目的：

- 判断提升到底来自哪里。
- 看模型是不是只在某些周期段更有效。

### 5.4 论文笔记的使用顺序

`paper/` 目录里的论文解读不再按“谁先写完谁先看”，而是按研究用途分层使用。

建议先看：

1. `TimeXer`
2. `PatchTST`
3. `TimeMixer`

再看：

1. `TiDE`
2. `TemporalFusionTransformer`
3. `iTransformer`

对应的完整优先级清单放在：

- `paper/reproduction_priority.md`

## 6. 可以怎么改论文里的方法

### 6.1 `TimeXer`

适合改的方向：

- 在注意力里加入周期偏置。
- 把 24 小时、168 小时作为显式先验。
- 对残差做单独注意力。
- 用更强的变量选择或 gating 过滤弱外生变量。

研究目标：

- 不是让模型“看见更多变量”，而是让它“先看懂周期，再修正残差”。

### 6.2 `TimeMixer`

适合借鉴的方向：

- 多尺度分解。
- 趋势/季节/残差拆分。
- 周期分支和短期扰动分支分开建模。

研究目标：

- 把“日周期主导”显式写进结构里。

### 6.3 `PatchTST`

适合借鉴的方向：

- patch 长度对齐 24 或 168。
- 把局部片段当作周期单元。

研究目标：

- 让 patch 切分更贴近负荷周期，而不是只做通用切块。

### 6.4 `TFT`

适合借鉴的方向：

- variable selection network。
- gating 机制。

研究目标：

- 用来判断哪些外生变量真的有用，哪些只是噪声。

### 6.5 `TiDE`

适合借鉴的方向：

- 先做去季节化，再做残差预测。
- 用较轻的残差头做修正。

研究目标：

- 适合当前这种“周期信号很强、残差才是难点”的任务。

### 6.6 `iTransformer`

适合借鉴的方向：

- 作为变量维度处理方式的对照。

研究目标：

- 不一定要作为主线，但可以作为横向比较，判断“变量建模方式”是否比“时间建模方式”更重要。

## 7. 推荐研究路线

1. 先把周期性证据画清楚。
2. 先把周期基线做稳。
3. 先用 `DLinear` 和 `PatchTST` 作为强对照。
4. 再做 `TimeXer` 的周期增强版。
5. 再试 `TimeMixer` / `TFT` / `TiDE` 这类“结构上更贴近周期和变量选择”的方法。
6. 最后才决定是否值得继续堆更复杂的 backbone。

## 8. 判断标准

一个新方法只有在下面至少一项上真正更好，才值得保留：

- 总体 `MSE` / `MAE` 更低。
- 在 `24/48/96` 三个 horizon 上都更稳。
- 在工作日、周末、季节切片上更稳。
- 对周期残差的解释更合理。
- 没有引入泄漏风险。

如果一个改动只是让训练更复杂，但没有更好地解释周期结构，就不优先推进。

## 9. 文档更新规则

- 只要研究假设、周期结论、特征边界、实验路线变化，就先改 `guide.md`。
- 只要代码、脚本、数据路径、产物位置变化，就改 `AGENTS.md`。
- 如果一个变化同时影响研究判断和实现方式，两份文档都要改，但不要重复写同一层级的内容。

## 10. 当前结论

- 净负荷研究应该从周期性出发。
- 当前最强对照基线是 `DLinear`。
- `TimeXer` 不是最强，但仍然值得做周期增强。
- 当前 NOAA 特征没有带来稳定提升。
- 下一步不是盲目加模型，而是把“周期表达 + 残差修正”这条线做实。
